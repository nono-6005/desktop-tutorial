import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// Built output is served directly by GitHub Pages under /{{REPO_NAME}}/{{APP_DIR}}/,
// so use a relative base and build straight into the sibling {{APP_DIR}}/ directory.
export default defineConfig({
  plugins: [react()],
  base: './',
  build: {
    outDir: '../{{APP_DIR}}',
    emptyOutDir: true,
  },
})
